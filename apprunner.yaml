version: 1.0
runtime: docker
build:
  commands:
    build:
      - echo "Using pre-built Docker image"
run:
  runtime-version: latest
  command: /app/scripts/run-prod.sh
  network:
    port: 8000
    env: PORT
  env:
    - name: PORT
      value: "8000"
